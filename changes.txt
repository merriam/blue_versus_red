CHANGES:  Display

The score increases on each collision.  This is a simple text entity, setting its property initially and in the onHit() function.  Note the text justifies to the shortest width, changing the word break as the score gets larger.